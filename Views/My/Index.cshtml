@model IEnumerable<MyModel> 

@{
    ViewData["Title"] = "Index";
}

<div class="container p-5">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-info">My mssql server table list</h2>
        </div>
        <div class="col-6 text-right">
            <h4 class="btn btn-outline-success"><a asp-controller="My" asp-action="Create">
                Add new table row ➕</a>
            </h4>
        </div>
    </div>
    <br />
    @if (Model.Count() > 0){
        <table class="table table-bordered table-striped" style="width: 100%">
            <thead>
                <tr>
                    <th>
                        #id ℹ️
                    </th>
                    <th>
                        Name 🎫
                    </th>
                    <th>
                        Price 💲
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.id</td>
                        <td>@item.name</td>
                        <td>@item.price BAM</td>
                        <td class="text-center">
                            <div class="btn-group" role="group">
                                <a class="btn btn-outline-primary mx-2" asp-route-id="@item.id" asp-controller="My" asp-action="Edit">
                                    Edit 📝</a>
                                <form method="post" asp-route-id="@item.id" asp-action="Delete">
                                     
                                    <input type="submit" class="btn btn-danger mx-2" value="Delete 🗑️"/>
                                
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p class="text-danger">Empty table!</p>
    }
</div>